"""EDGAR integration tests — placeholder.

SPEC §3 includes this file in the layout; real tests come in Phase 2+ when
network-touching integration tests are reasonable to run in CI."""

import pytest


@pytest.mark.skip(reason="Phase 2+: EDGAR integration tests not yet written")
def test_edgar_placeholder():
    pass
